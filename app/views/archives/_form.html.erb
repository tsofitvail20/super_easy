<% if @archive.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@archive.errors.count, "error") %> prohibited
      this item from being saved:
    </h2>
    <ul>
      <% @archive.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
    </ul>
  </div>
<% end %>
